{
  "managedBy": "SkillsShare",
  "agent": "cursor",
  "repoRoot": "{{REPO_ROOT}}",
  "homeDir": "{{HOME_DIR}}",
  "executables": {
    "node": "{{NODE_PATH}}",
    "python": "{{PYTHON_PATH}}"
  },
  "mcpServers": {}
}
